"""
findata.server — FastAPI + DB layer for commercial API deployment.

This package contains all database persistence, API route handlers,
and ingestion pipelines. It is only needed when running the server,
not for library-only usage.

Start the server:
    python -m findata.server.app
"""
